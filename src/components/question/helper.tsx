import { JsxElement } from "typescript";

const generateInputElem = (currentQuestion: any) => {
  let inputElem = <div></div>;
  switch (currentQuestion) {
    case currentQuestion?.type:
      break;

    default:
      break;
  }
  return inputElem;
};

export { generateInputElem };
